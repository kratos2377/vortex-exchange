[package]
name = "pyth"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "pyth"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = ["devnet"]
devnet=[]

[dependencies]
anchor-lang = "0.30.1"
arrayref = "0.3.6"
bytemuck = { version = "1.4.0" }